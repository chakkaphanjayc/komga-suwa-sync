# =================================================================
# Komga-Suwayomi Sync Service Configuration
# =================================================================
# Copy this file to .env and fill in your actual values

# =================================================================
# Komga Server Configuration
# =================================================================
# Your Komga server URL with protocol and port
KOMGA_BASE=http://komga:25600
# Komga authentication credentials
KOMGA_USER=<komga_user>
KOMGA_PASS=<komga_pass>

# =================================================================
# Suwayomi Server Configuration
# =================================================================
# Your Suwayomi (Tachidesk) server URL with protocol and port
SUWA_BASE=http://suwayomi:4567

# Authentication - Choose ONE method:
# Option 1: Bearer Token (recommended - more secure)
SUWA_TOKEN=<your-suwayomi-bearer-token>

# Option 2: Basic Authentication (username/password)
# Uncomment and fill these if you prefer basic auth instead of token
# SUWA_USER=<suwayomi_user>
# SUWA_PASS=<suwayomi_pass>

# =================================================================
# Sync Configuration
# =================================================================
# How often to check for updates (in milliseconds)
# 60000 = 1 minute, 300000 = 5 minutes, 900000 = 15 minutes
SYNC_INTERVAL_MS=60000

# Fuzzy matching sensitivity for manga title matching (0.0 to 1.0)
# Lower values = more strict matching, Higher values = more lenient
# Recommended: 0.85 for most users
FUZZY_THRESHOLD=0.85

# Logging level: error, warn, info, debug
# Use 'debug' for troubleshooting, 'info' for normal operation
LOG_LEVEL=info

# Dry run mode - set to 'true' to test without making actual changes
# Useful for testing configuration before enabling real sync
SYNC_DRY_RUN=false

# =================================================================
# Advanced Configuration (usually don't need to change)
# =================================================================
# Database file location (relative to data directory)
# DB_PATH=./data/sync.db
